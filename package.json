{
  "name": "sevens-json-server",
  "version": "1.0.0",
  "description": "Deploy JSON Server to Render",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch /data/db.json --port $PORT"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "json-server": "^0.17.0",
    "json-server-auth": "^2.1.0"
  }
}
